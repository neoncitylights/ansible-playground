---
  - name: "My first playbook"
    hosts: localhost

    tasks:
      - name: "test reachability"
        ping:
      - name: "install stress"
        brew:
          name: stress
          state: present
